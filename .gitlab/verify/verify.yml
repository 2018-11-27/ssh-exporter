releaseVerify:
  image: $BASE_VERIFY_IMAGE #harbor.lenovo.com/base/verify:latest
  stage: verify
  rules: !reference [.defaultRules, branches]
  script:
  # You should add or modify your verify commands in this scope
  # Start
  # - fixme
    - echo "project name $CI_PROJECT_NAME branch $CI_COMMIT_REF_SLUG Verify stage sucessfully"
  # End
  tags:
    - mec-runner


masterVerify:
  image: $BASE_VERIFY_IMAGE #harbor.lenovo.com/base/verify:latest
  stage: verify
  script:
  # You should add or modify your verify commands in this scope
  # Start
  # - fixme
    - echo "project name $CI_PROJECT_NAME branch $CI_COMMIT_REF_SLUG Verify stage sucessfully"
  # End
  tags:
    - mec-runner
  only:
    - master

mRVerify:
  image: $BASE_VERIFY_IMAGE #harbor.lenovo.com/base/verify:latest
  stage: verify
  script:
  # You should add or modify your verify commands in this scope
  # Start
  # - fixme
    - echo "project name $CI_PROJECT_NAME branch $CI_COMMIT_REF_SLUG Verify stage sucessfully"
  # End
  tags:
    - mec-runner
  only:
    - merge_requests

stagingVerify:
  image: $BASE_VERIFY_IMAGE #harbor.lenovo.com/base/verify:latest
  stage: verify
  script:
  # You should add or modify your verify commands in this scope
  # Start
  # - fixme
    - echo "project name $CI_PROJECT_NAME branch $CI_COMMIT_REF_SLUG Verify stage sucessfully"
  # End
  tags:
    - mec-runner
  only:
    - /^staging-.*$/

othersVerify:
  image: $BASE_VERIFY_IMAGE  #harbor.lenovo.com/base/verify:latest
  stage: verify
  script:
  # You should add or modify your verify commands in this scope
  # Start
  # - fixme
    - echo "project name $CI_PROJECT_NAME branch $CI_COMMIT_REF_SLUG Verify stage sucessfully"
  # End
  tags:
    - mec-runner
  except:
    - master
    - /^staging-.*$/
    - /^release-.*$/
    - merge_requests